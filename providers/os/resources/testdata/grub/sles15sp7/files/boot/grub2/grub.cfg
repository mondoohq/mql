#
# DO NOT EDIT THIS FILE
#
# It is automatically generated by grub2-mkconfig using templates
# from /etc/grub.d and settings from /etc/default/grub
#

### BEGIN /etc/grub.d/00_header ###
set btrfs_relative_path="n"
export btrfs_relative_path
if [ -f ${config_directory}/grubenv ]; then
  load_env -f ${config_directory}/grubenv
elif [ -s $prefix/grubenv ]; then
  load_env
fi

if [ "${env_block}" ] ; then
  set env_block="(${root})${env_block}"
  export env_block
  load_env -f "${env_block}"
fi

if [ "${next_entry}" ] ; then
   set default="${next_entry}"
   set next_entry=
   save_env next_entry
   if [ "${env_block}" ] ; then
     save_env -f "${env_block}" next_entry
   fi
   set boot_once=true
else
   set default="0"
fi

if [ x"${feature_menuentry_id}" = xy ]; then
  menuentry_id_option="--id"
else
  menuentry_id_option=""
fi

export menuentry_id_option

if [ "${prev_saved_entry}" ]; then
  set saved_entry="${prev_saved_entry}"
  save_env saved_entry
  set prev_saved_entry=
  save_env prev_saved_entry
  set boot_once=true
fi

function savedefault {
  if [ -z "${boot_once}" ]; then
    saved_entry="${chosen}"
    if [ "${env_block}" ] ; then
      save_env -f "${env_block}" saved_entry
    else
      save_env saved_entry
    fi

  fi
}

function load_video {
  if [ x$feature_all_video_module = xy ]; then
    insmod all_video
  else
    insmod efi_gop
    insmod efi_uga
    insmod ieee1275_fb
    insmod vbe
    insmod vga
    insmod video_bochs
    insmod video_cirrus
  fi
}

if [ x$feature_default_font_path = xy ] ; then
   font=unicode
else
insmod part_gpt
insmod xfs
search --no-floppy --fs-uuid --set=root 5dbbd159-9bef-4f1f-9d52-b5ee8606e719
    font="/usr/share/grub2/unicode.pf2"
fi

if loadfont $font ; then
  if [ "${grub_platform}" = "efi" ]; then
    clear
    echo "Please press 't' to show the boot menu on this console"
  fi
  set gfxmode=auto
  load_video
  insmod gfxterm
  set locale_dir=$prefix/locale
  set lang=en_US
  insmod gettext
fi
terminal_input console

for i in gfxterm; do
  if [ x${use_append} = xtrue ]; then
     terminal_output --append $i
  elif terminal_output $i; then
     use_append=true;
  fi
done

if [ x${boot_once} = xtrue ]; then
  set timeout=0
elif [ x$feature_timeout_style = xy ] ; then
  set timeout_style=menu
  set timeout=1
# Fallback normal timeout code in case the timeout_style feature is
# unavailable.
else
  set timeout=1
fi
### END /etc/grub.d/00_header ###

### BEGIN /etc/grub.d/05_crypttab ###
### END /etc/grub.d/05_crypttab ###

### BEGIN /etc/grub.d/10_linux ###
menuentry 'SLES15-SP7'  --class sles15_sp7 --class gnu-linux --class gnu --class os $menuentry_id_option 'gnulinux-simple-5dbbd159-9bef-4f1f-9d52-b5ee8606e719' {
	load_video
	set gfxpayload=keep
	insmod gzio
	insmod part_gpt
	insmod xfs
	search --no-floppy --fs-uuid --set=root 5dbbd159-9bef-4f1f-9d52-b5ee8606e719
	echo	'Loading Linux 6.4.0-150700.53.73-default ...'
	linux	/boot/vmlinuz-6.4.0-150700.53.73-default  root=LABEL=ROOT console=ttyS0 net.ifnames=0 iommu.passthrough=1 8250.nr_uarts=4 dis_ucode_ldr multipath=off nvme_core.admin_timeout=4294967295 nvme_core.io_timeout=4294967295 systemd.unified_cgroup_hierarchy=1
	echo	'Loading initial ramdisk ...'
	initrd	/boot/initrd-6.4.0-150700.53.73-default
}
submenu 'Advanced options for SLES15-SP7' --hotkey=1 $menuentry_id_option 'gnulinux-advanced-5dbbd159-9bef-4f1f-9d52-b5ee8606e719' {
	menuentry 'SLES15-SP7, with Linux 6.4.0-150700.53.73-default' --hotkey=2 --class sles15_sp7 --class gnu-linux --class gnu --class os $menuentry_id_option 'gnulinux-6.4.0-150700.53.73-default-advanced-5dbbd159-9bef-4f1f-9d52-b5ee8606e719' {
		load_video
		set gfxpayload=keep
		insmod gzio
		insmod part_gpt
		insmod xfs
		search --no-floppy --fs-uuid --set=root 5dbbd159-9bef-4f1f-9d52-b5ee8606e719
		echo	'Loading Linux 6.4.0-150700.53.73-default ...'
		linux	/boot/vmlinuz-6.4.0-150700.53.73-default  root=LABEL=ROOT console=ttyS0 net.ifnames=0 iommu.passthrough=1 8250.nr_uarts=4 dis_ucode_ldr multipath=off nvme_core.admin_timeout=4294967295 nvme_core.io_timeout=4294967295 systemd.unified_cgroup_hierarchy=1
		echo	'Loading initial ramdisk ...'
		initrd	/boot/initrd-6.4.0-150700.53.73-default
	}
	menuentry 'SLES15-SP7, with Linux 6.4.0-150700.53.73-default (recovery mode)' --hotkey=3 --class sles15_sp7 --class gnu-linux --class gnu --class os $menuentry_id_option 'gnulinux-6.4.0-150700.53.73-default-recovery-5dbbd159-9bef-4f1f-9d52-b5ee8606e719' {
		load_video
		set gfxpayload=keep
		insmod gzio
		insmod part_gpt
		insmod xfs
		search --no-floppy --fs-uuid --set=root 5dbbd159-9bef-4f1f-9d52-b5ee8606e719
		echo	'Loading Linux 6.4.0-150700.53.73-default ...'
		linux	/boot/vmlinuz-6.4.0-150700.53.73-default  single root=LABEL=ROOT
		echo	'Loading initial ramdisk ...'
		initrd	/boot/initrd-6.4.0-150700.53.73-default
	}
}

### END /etc/grub.d/10_linux ###

### BEGIN /etc/grub.d/20_linux_xen ###

### END /etc/grub.d/20_linux_xen ###

### BEGIN /etc/grub.d/30_os-prober ###
### END /etc/grub.d/30_os-prober ###

### BEGIN /etc/grub.d/30_uefi-firmware ###
if [ "$grub_platform" = "efi" ]; then
	menuentry 'UEFI Firmware Settings' $menuentry_id_option 'uefi-firmware' {
                fwsetup --is-supported
                if [ "$?" = 0 ]; then
                        fwsetup
                else
                        echo "Your firmware doesn't support setup menu entry from a boot loader"
                        echo "Press any key to return ..."
                        read
                fi
        }
fi
### END /etc/grub.d/30_uefi-firmware ###

### BEGIN /etc/grub.d/40_custom ###
# This file provides an easy way to add custom menu entries.  Simply type the
# menu entries you want to add after this comment.  Be careful not to change
# the 'exec tail' line above.
### END /etc/grub.d/40_custom ###

### BEGIN /etc/grub.d/41_custom ###
if [ -f  ${config_directory}/custom.cfg ]; then
  source ${config_directory}/custom.cfg
elif [ -z "${config_directory}" -a -f  $prefix/custom.cfg ]; then
  source $prefix/custom.cfg
fi
### END /etc/grub.d/41_custom ###

### BEGIN /etc/grub.d/90_persistent ###
### END /etc/grub.d/90_persistent ###

### BEGIN /etc/grub.d/95_textmode ###
if [ "${grub_platform}" = "efi" ]; then
  # On EFI systems we can only have graphics *or* serial, so allow the user
  # to switch between the two
  hiddenentry 'Text mode' --hotkey 't' {
    set textmode=true
    terminal_output console
  }
fi
### END /etc/grub.d/95_textmode ###
